{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Configure Phonehome",
    "$id": "http://nethserver.org/json-schema/task/input/phonehome/configure",
    "description": "Configure Phonehome",
    "examples": [
      {
        "hostname": "phonehome.nethserver.org",
        "geoip_token": "XXXXXXXXXXX",
        "log_level": "error",
        "debug": false,
        "http_to_https": true,
        "lets_encrypt": true
      }
    ],
    "type": "object",
    "required": [
      "hostname",
      "geoip_token",
      "http_to_https",
      "lets_encrypt"
    ],
    "properties": {
      "hostname": {
        "type": "string",
        "description": "Host name for phonehome instance, like 'wiki.nethserver.org'.",
        "format": "idn-hostname"
      },
      "geoip_token": {
        "type": "string",
        "description": "Token provided by MaxMind to fetch GeoIp2 Country Database."
      },
      "log_level": {
        "type": "string",
        "description": "Log level that the application should output.",
        "enum": [
          "emergency",
          "alert",
          "critical",
          "error",
          "warning",
          "notice",
          "info",
          "debug"
        ]
      },
      "debug": {
        "type": "boolean",
        "description": "If the application should enable debug mode, beware, if something happens, all stack will be printed to the page."
      },
      "lets_encrypt": {
        "type": "boolean",
        "title": "Let's Encrypt certificate",
        "description": "Request a valid Let's Encrypt certificate."
      },
      "http_to_https": {
        "type": "boolean",
        "title": "HTTP to HTTPS redirection",
        "description": "Redirect all the HTTP requests to HTTPS"
      }
    }
  }
